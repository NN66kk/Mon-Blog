# Mon 博客写作操作指南

适用：当前已上线的写作室管理平台。整理日期：2026-09-22。

日常使用这两个地址：

- [写作室：写文章和管理内容](https://mon-blog-writer.monv587.chatgpt.site/)
- [公开博客：查看读者看到的效果](https://nn66kk.github.io/Mon-Blog/)

收藏线上写作室地址即可。`localhost:5174` 是临时本地开发地址，不作为日常写作入口。

## 1. 每次写新文章，按这个顺序

**打开写作室 → 按模板新建 → 逐项填写 YAML 信息 → 写正文 → 预览 → 发布文章 → 确认已上线 → 本地拉取。**

### 第一步：登录并新建

1. 打开写作室，使用自己的 ChatGPT 账号登录。
2. 点击「新文章」或「写一篇新文章」。
3. 新草稿默认按你项目的 `002-project/005-template/yaml.md` 模板生成，包含 YAML 和正文一级标题。
4. 填写上方「文章标题 · YAML title」。正文的模板一级标题会随之更新；你单独改写过的一级标题会保留。

继续上次未写完的文章时，从「草稿箱」找到它，点击「继续写」，无需重新创建。

### 第二步：填写文章信息

标题下面直接显示 YAML 表单，电脑和手机都可以逐项填写，不必手写 YAML 语法。

| 项目 | 怎么填写 |
| --- | --- |
| 文章标题 · title | 读者看到的真正标题，与上方标题框及 YAML 同步 |
| 文章文件名 | 自动生成 12 位时间编号，例如 `260814185045.md`；不是标题，不必手填 |
| 文章日期 · date | 使用北京时间，自动填写，可修改；写入格式为 `2026-08-14T18:50:45+08:00` |
| 收录栏目 | 发布前选好，按下方用途参考选择 |
| 文章标签 · tags | 默认使用对应栏目标签；多个标签用逗号分隔，例如 `原创文章，AI，工具` |
| 文章摘要 · description | 用一两句话概括内容；也可以留空，由博客从正文提取 |
| 完整 YAML | 点击「编辑完整 YAML」或工具栏「YAML」，可修改其他字段；原有自定义字段会保留 |

栏目用途参考：

| 栏目 | 适合内容 | 对应本地目录 |
| --- | --- | --- |
| 原创手记 | 自己的观点、随笔、经验总结 | `docs/D-Orginals/` |
| 格物札记 | 技术、学习笔记、方法记录 | `docs/B-Notes/` |
| 行旅札记 | 旅行、生活观察、见闻 | `docs/A-Life/` |
| 人间拾遗 | 摘录、收藏、阅读所得 | `docs/C-Highlights/` |

最新文章使用 `YYMMDDHHmmss.md` 命名：`260814185045.md` 表示北京时间 2026-08-14 18:50:45。写作室首次保存草稿时分配编号，同一秒创建多篇会自动避让。之后改标题、日期或恢复历史内容，都不会重新生成编号。修改已发布文章保留原文件路径（包括历史中文文件名、UUID 文件名），也不能直接换栏目，以保持原有链接。

栏目默认标签依次是「原创文章」「学习笔记」「生活专栏」「优质内容」。换栏目时只自动切换未改写的默认标签，你自己添加的标签会保留。

旧文章使用 `cdate` 或 `published_at` 时，日期表单会标明并继续编辑该原字段。完整 YAML 格式暂时有错时，草稿仍可保存和导出，但需修正后才能发布。

### 第三步：写正文和插入图片

- 正文使用 Markdown；工具栏可插入加粗、二级标题、链接和代码块。
- 图片可以点击「上传图片」，也可以粘贴截图或拖入图片。
- 重复使用写作室内的图片时，点击工具栏「从图片库插入」。
- 支持 PNG、JPEG、WebP、GIF。单张上传上限 5 MB；一次发布最多包含 12 张私有上传图片，合计不超过 20 MB。大图先压缩会更顺畅。
- 从本地导入 Markdown 时，本机磁盘上的图片需要重新上传，不能只留下 `C:\...` 这样的地址。

先把光标放到希望插图的位置，再上传或插入。之后切换「预览」确认图片能正常显示。

### 第四步：确认保存

写作室会自动保存；也可以按 `Ctrl+S`（Mac 为 `Cmd+S`），或点击「保存草稿」。离开页面前等到状态显示「已保存」。

| 状态 | 表示什么 |
| --- | --- |
| 已保存 | 内容已保存到私人草稿，公开博客还没有更新 |
| 等待保存／正在保存 | 保存尚未完成，稍等后再离开 |
| 仅保存在本机／保存报错 | 尚不能当作云端备份；先保留当前页面并导出内容 |
| 文章已上线／已上线 | 已确认公开博客包含这次发布内容 |

如果暂时不想公开，保存草稿后即可离开，下次从草稿箱继续。不要为了保存而点击「发布文章」。

### 第五步：预览后发布

1. 切换到「预览」，检查段落、图片、链接、代码和公式。
2. 核对 title、date、tags、description 和收录栏目；文件名与标题是两个独立概念。
3. 点击一次右上角「发布文章」。
4. 在「文章设置」里的发布状态，或管理中心「发布记录」中查看进度。
5. 等待「文章已上线」或「已上线」，再打开公开文章核对效果。

提交成功后，博客还需要自动构建和更新。看到「等待构建」「部署中」或「构建完成，等待上线」时，继续等待并刷新状态即可；不要连续点击发布。

Wiki Link、Mermaid 和 MkDocs 特殊语法的最终效果，以公开博客为准。普通 Markdown 和图片可以先在编辑器里检查。

### 第六步：把发布结果同步到电脑

如果你还要使用本地仓库或 Obsidian：

1. 打开 GitHub Desktop，选择 `Mon-Blog` 和 `main`。
2. 点击「Fetch origin」。
3. 有「Pull origin」时点击拉取。
4. 本地仓库就能拿到这次发布的文章和图片。

仅用手机或网页写作时，不必每次立即开电脑；下次开始修改本地文件之前，再完成拉取。

## 2. 修改已经发布的文章

1. 进入「博客文章」，点击「同步博客」。
2. 搜索标题或筛选栏目，找到文章后点击「编辑」。
3. 修改正文、标题、标签或摘要，确认「已保存」。
4. 预览无误后，仍然点击「发布文章」。这里会更新原文章。
5. 等待上线，打开原文章链接检查；需要使用本地仓库时再拉取。

编辑旧文会沿用原文件路径和原链接，并保留原有文章信息。不要用「新文章」或「复制草稿」代替旧文编辑，这两种操作会创建独立的新稿。

## 3. 本地与写作室怎样配合

建议日常文章主要用写作室完成，本地主要维护博客代码、样式和配置。需要用 Obsidian 写文章时，也可以按下面的顺序协作。

| 场景 | 正确顺序 |
| --- | --- |
| 在写作室发布后，准备改本地内容 | GitHub Desktop：Fetch → Pull → 再开始修改 |
| 在本地写完，准备同步到博客 | 保存文件 → 检查 Changes → 提交 → Fetch → 有远端更新先合并 → Push |
| 本地推送后，准备回写作室继续编辑 | 写作室「博客文章」→「同步博客」→ 重新打开文章最新版 |
| GitHub Desktop 显示上下箭头都有数字 | 两边都有新提交；先合并历史，再普通推送。若出现冲突，保留双方内容后再处理 |
| 只保存了线上草稿，还没发布 | 草稿在云端数据库，本地 Git 拉取不会得到这份草稿；需要时单独导出 |

同一篇文章尽量不要同时在本地、网页或两台设备上编辑。切换写作位置前，先完成前一处的保存和同步。

GitHub Desktop 的「No local changes」只表示没有未提交的文件改动。还要查看顶部是否有待拉取或待推送的提交，才能判断本地与 GitHub 是否一致。

文章发布通过 GitHub 自动构建；日常操作无需在本地运行 `mkdocs build`。修改写作室程序本身则需要另行部署 Sites，和发布博客文章是两件事。

## 4. 出现冲突或报错时怎么做

| 遇到的情况 | 处理顺序 |
| --- | --- |
| 提示另一台设备保存了新版本 | 先用顶部「导出」保存当前 Markdown，再刷新并重新打开云端草稿，合并自己的修改后保存 |
| 提示文章已在其他地方修改 | 先导出当前内容；回到「博客文章」同步并打开最新文章，把这次修改合并进去，再发布 |
| 网络断开或一直保存失败 | 先导出 Markdown 保住当前文字；联网后再保存。普通导出只有图片引用，不包含图片文件 |
| GitHub 权限不足 | 到「设置与备份」检查连接；令牌应授权 `Mon-Blog`，Contents 为 Read and write，Actions 为 Read-only |
| GitHub 请求受限 | 暂停同步和重复发布，稍后重试；保留当前草稿 |
| 博客构建失败 | 打开「发布记录」中的 GitHub Actions／构建链接查看错误，修正后保存新版本再发布 |
| 已提交但博客还是旧内容 | 查看发布记录，等状态确认上线；上线后仍显示旧内容，可刷新文章页 |

如果发布任务已经明确失败，修正问题后保存一个有实际内容变化的新修订，再发起发布；反复提交同一草稿修订会返回原任务结果。

遇到无法自行判断的冲突，可以把提示和相关文件路径发给我。不要先强制推送、丢弃改动或删除本地文件。

## 5. 删除、恢复和找回历史

| 你想做什么 | 操作方式 | 对公开博客的影响 |
| --- | --- | --- |
| 清理不需要的草稿 | 草稿箱 → 删除草稿／移入回收站 | 已发布文章仍保留 |
| 恢复误删草稿 | 回收站 → 恢复草稿 | 恢复到草稿箱，不会自动发布 |
| 删除公开文章 | 博客文章 → 删除图标 → 输入文章标题确认 | 提交删除，等待发布记录显示「已下线」 |
| 恢复已删除的公开文章 | 回收站 → 找到文章 → 确认恢复 | 恢复到原路径并触发部署；同路径已有内容时会停止 |
| 找回旧的草稿内容 | 写作页 →「草稿历史版本」→ 选择版本恢复 | 只恢复编辑稿；更新博客仍需发布 |
| 找回旧的公开文章内容 | 博客文章 →「文章历史」→「打开此版本」 | 创建独立编辑稿，审阅并发布后才更新博客 |

图片归档会保留原文件；仍被草稿（包括回收站草稿）引用的图片不能归档。公开博客图片目前提供浏览，不做自动批量删除。

## 6. 什么时候备份，备份什么

建议每周一次，或在长文完成、大幅改写之前做一次备份。

| 备份方式 | 入口 | 包含内容 |
| --- | --- | --- |
| 当前文章快速导出 | 写作页顶部「导出」 | Markdown 和图片引用，不含图片附件；适合出错时先保住文字 |
| 当前文章和图片 | 写作页「文章设置」→「导出文章与图片」 | 当前文章及引用的私有上传图片，ZIP 格式 |
| 所选草稿备份 | 草稿箱勾选文章 →「导出所选」 | 所选草稿及引用的私有图片 |
| 完整草稿备份 | 设置与备份 →「导出完整草稿备份」 | 全部当前草稿、回收站草稿及引用的私有图片 |

ZIP 不包含所有历史修订或整个博客仓库。已经发布的文章与原有图片由 GitHub 保管；拉取到本地可得到当前版本。

把下载的 ZIP 放到自己的备份目录。当前支持导入 Markdown，尚不支持整包 ZIP 一键恢复；重新导入时，解压后的本地图片需要重新上传。

## 7. 使用项目里的文章模板

你当前的标准模板输出如下（日期与标题为示例）：

```markdown
---
title: "文章真正的标题"
date: 2026-09-22T18:30:00+08:00
tags: 原创文章
description: "一两句话概括文章内容。"
---

# 文章真正的标题

在这里写正文。
```

在写作室逐项填表即可生成上述 YAML；正文编辑区只写 `# 标题` 以下的内容，无需再粘贴一份 YAML。完整 YAML 编辑区也不要求输入两端的 `---`，粘贴一个完整且闭合的 YAML 头部时会自动去掉边界。

顶部「模板」提供两种实际项目模板：

- **标准文章 · YAML 与一级标题**：先保存当前文章，再创建独立的新草稿；不会覆盖当前正文。
- **社群 · 正文片段**：预览内容后点击「插入当前正文」，插入光标位置；不是自动附加到每篇文章。

本地新增或修改 `002-project/005-template/` 中的 Markdown 模板后，先提交并推送到 GitHub，再到写作室「模板 → 同步仓库模板」。含 YAML 的普通 Markdown 作为新文章模板，无 YAML 的普通 Markdown 作为正文片段。你的现有 `yaml.md` 已适配；其他 Obsidian/Templater 脚本不会在网页中执行，需要另存为普通 Markdown 后使用。默认「新文章」使用内置的当前标准模板；同步后要使用其他模板，请从模板列表选择。

普通 Markdown 导出沿用保存后的文章文件名。发布时还会由写作室维护 `updated_at`、`status` 等状态信息，不必手填。

## 8. 发布前后速查

- [ ] 标题清楚，栏目正确，标签和摘要已检查。
- [ ] 正文、图片、链接已预览。
- [ ] 草稿显示「已保存」。
- [ ] 点击一次「发布文章」，等待状态确认上线。
- [ ] 打开公开文章检查最终效果。
- [ ] 下次修改本地仓库前完成 Fetch／Pull。
- [ ] 长文或重要改写已另存备份。
