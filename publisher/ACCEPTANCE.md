# 写作室验收记录

日期：2026-09-22。范围：现有写作后台的保存、预览和发布正确性。

## 本轮修复

- 切换、导入、恢复草稿统一经过过渡锁：先完成当前保存，再加载目标；等待期间禁止编辑及重复切换，失败保留原稿。
- 被取消的构建仍检查文章 release 标记；同一文章较新版本确认上线后，旧任务结束为 `superseded`。轮询按批轮转，避免旧任务被饿死。
- 首次发布日期仅在提交成功后持久化。结果不确定时，从对应 commit 恢复日期；失败尝试不占用日期。
- 连接配置就绪判断与实际加密要求一致，少于 32 字符的配置不会显示为就绪。
- 根 README 对齐现用拆分样式、日期优先级和不运行本地 MkDocs 构建的约定。

## 已通过

| 检查 | 结果 |
| --- | --- |
| `npm test` | 19 项通过；GitHub 写入使用模拟调用，不访问真实仓库 |
| `npm run test:local` | 6 项通过；仅连接本机开发服务 |
| `npx tsc --noEmit --incremental false` | 通过 |
| `npm run build` | 通过，仅构建 publisher |
| 实际页面操作 | 本地登录、草稿保存/重新打开、Markdown/公式/任务列表预览、图片上传插入、切换前末句保留 |

本地接口检查覆盖匿名拒绝、跨站写入拒绝、草稿版本冲突、非法栏目、图片字节往返、私有图片访问与伪造格式拒绝。测试数据留在被 Git 忽略的本机 D1/R2 中。

构建仍有非阻断提示：Vite 未来默认配置加载器要求 JSON import attributes，部分客户端包大于 500 kB。本轮没有调整依赖或构建配置。

## 已核实的线上基础状态

- 当前 Sites 版本号为 2，访问策略仅列出所有者，没有共享用户组。
- 已有 `DB` 绑定以及 `connections`、`drafts`、`publications` 表。
- `CREDENTIAL_KEY` 已作为 secret 配置；本次没有读取或更改其值。
- 所有者随后已在 Edge 登录并连接 GitHub，用户提供的截图显示「博客已连接」。本轮尚未通过受控浏览器独立复核。

以上不等于本轮代码已上线，也不等于真实发布验收通过。

## 浏览器完整验收补充

2026-09-22：已开始连接用户登录的 Edge。扩展已启用、浏览器运行和本机连接注册均正常；重开窗口后控制通道仍无响应。后续按用户要求改用已连接的 Browser Use。

Browser Use 已实际打开线上写作室，首先显示 `Sign in required` 和 `Continue with ChatGPT`；点击登录入口后进入 Google 登录页，未自动继承本地 Edge 的登录状态。已请所有者在云端浏览器亲自完成登录。尚未对线上写作室执行编辑、上传、导入或发布操作，也未输入凭据或更改连接设置。

等待连接恢复期间，离线复现并修复：

- 非对象 YAML（如 `hello`、`null` 或列表）使标签编辑/导出抛异常：现在给出对象格式提示，导出仍保留原始 YAML 和正文以便恢复。
- 无效图片地址（如 `//` 或 `http:`）使预览抛异常：现在显示图片地址无效，继续保留编辑器。
- 冲突错误说明改为「导出备份 → 刷新页面 → 重新打开云端草稿」，避免提示用户进入反复保存冲突的循环。
- 导出含私有图片的草稿时明确提示：Markdown 包含图片引用，但不包含图片附件。

上述修复的单元测试和类型检查已通过，浏览器验证待连接恢复后执行。原有线上版本尚未包含这些修复。

待逐项实测：新建及自动保存、刷新恢复、草稿搜索及切换、标签/摘要/高级 YAML、编辑工具栏、Markdown/公式预览、图片上传/预览、导出文件、已有文章导入与元数据保留、两个标签页保存冲突、手机布局和键盘操作。真实发布与更新文章会向 GitHub 创建提交，必须在用户批准后执行。

## 待批准并执行的生产验收

1. 提交已审阅的后台源码与文档；按需推送并更新现有私有写作室，保持仅所有者访问。
2. 所有者在写作室登录并连接 GitHub（若尚未连接），令牌仅在写作室界面输入。
3. 审阅 `work/production-acceptance.md` 的候选文章。批准后，在原创手记栏目发布该文，并上传现有博客封面验证图片链路。
4. 核对一次提交包含文章与图片、GitHub Actions 结果、页面 release 标记和实际渲染；再导入该文进行一次已批准的更新，核对原路径和首次日期保持不变。

本轮未创建 Git commit、未推送、未更改线上配置、未部署、未发布公开文章。生产验收前必须获得项目约定要求的明确批准。候选文章在被忽略的 `work/` 中，不会被博客构建或自动发布。
